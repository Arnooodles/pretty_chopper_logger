name: example
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  chopper: ^8.1.0
  # Pretty chopper logger (local package)
  pretty_chopper_logger:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  chopper_generator: ^8.1.0
  build_runner: ^2.5.4

flutter:
  uses-material-design: true

